<main>
  <ul>
    <% sentEmails.forEach(function(email) { %>
      <li>
        <%= email.senderFullName %>,
          <%= email.senderEmail %>,
            <%= email.subject %>,
              <%= email.body %>
      </li>
      <% }); %>
  </ul>
</main>
