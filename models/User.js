class User {
  constructor (db) {
    this.db = db
  }

  create(user) {
    // Code to create a new user in the database
  }

  read(id) {
    // Code to read a user from the database
  }

  update(id, user) {
    // Code to update a user in the database
  }

  delete(id) {
    // Code to delete a user from the database
  }
}
module.exports = User
